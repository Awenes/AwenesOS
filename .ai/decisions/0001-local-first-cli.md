# ADR 0001: Local-first CLI before dashboard

Status: accepted

Use a Node.js/TypeScript CLI backed by SQLite. This gives the work loop an executable interface with low operational cost and keeps the domain reusable for a future tray app or dashboard.
